<Window x:Class="JCLib.VisualStudio.PackCreationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="JC Lib — création de pack"
        Width="610"
        Height="390"
        MinWidth="540"
        MinHeight="350"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">
    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#252526" />
        <SolidColorBrush x:Key="PanelBrush" Color="#2D2D30" />
        <SolidColorBrush x:Key="BorderBrush" Color="#666666" />
        <SolidColorBrush x:Key="InputBrush" Color="#1E1E1E" />
        <SolidColorBrush x:Key="DropdownBackgroundBrush" Color="#1E1E1E" />
        <SolidColorBrush x:Key="DropdownTextBrush" Color="#F1F1F1" />
        <SolidColorBrush x:Key="TextBrush" Color="#F1F1F1" />
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#C8C8C8" />
        <SolidColorBrush x:Key="AccentBrush" Color="#8FC7FF" />
        <SolidColorBrush x:Key="ButtonTextBrush" Color="#111111" />
        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="{DynamicResource TextBrush}" /></Style>
        <Style TargetType="Button"><Setter Property="Padding" Value="12,6" /><Setter Property="Margin" Value="0,0,6,0" /><Setter Property="Foreground" Value="{DynamicResource ButtonTextBrush}" /></Style>
        <Style TargetType="TextBox"><Setter Property="Background" Value="{DynamicResource InputBrush}" /><Setter Property="Foreground" Value="{DynamicResource TextBrush}" /><Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" /><Setter Property="CaretBrush" Value="{DynamicResource TextBrush}" /></Style>
        <Style x:Key="DropdownComboBoxItemStyle" TargetType="ComboBoxItem"><Setter Property="Background" Value="{DynamicResource DropdownBackgroundBrush}" /><Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" /><Setter Property="Padding" Value="5,3" /></Style>
        <Style TargetType="ComboBox"><Setter Property="Background" Value="{DynamicResource DropdownBackgroundBrush}" /><Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" /><Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" /><Setter Property="ItemContainerStyle" Value="{StaticResource DropdownComboBoxItemStyle}" /></Style>
    </Window.Resources>
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="DialogTitleText" FontSize="18" FontWeight="SemiBold" Text="Créer un pack JC Lib" />
        <TextBlock x:Name="DialogDescriptionText" Grid.Row="1" Margin="0,5,0,12" Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap" />

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="150" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <TextBlock Margin="0,5,8,0" FontWeight="SemiBold" Text="Destination" />
            <ComboBox x:Name="DestinationComboBox" Grid.Column="1" Padding="4" DisplayMemberPath="Label" />
        </Grid>
        <Grid Grid.Row="3" Margin="0,0,0,8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="150" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <TextBlock Margin="0,5,8,0" FontWeight="SemiBold" Text="Identifiant" />
            <TextBox x:Name="PackIdTextBox" Grid.Column="1" Padding="5,3" />
        </Grid>
        <Grid Grid.Row="4" Margin="0,0,0,8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="150" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <TextBlock Margin="0,5,8,0" FontWeight="SemiBold" Text="Nom affiché" />
            <TextBox x:Name="PackNameTextBox" Grid.Column="1" Padding="5,3" />
        </Grid>
        <Grid Grid.Row="5" Margin="0,0,0,8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="150" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <TextBlock Margin="0,5,8,0" FontWeight="SemiBold" Text="Version" />
            <TextBox x:Name="PackVersionTextBox" Grid.Column="1" Padding="5,3" />
        </Grid>
        <Grid Grid.Row="6" Margin="0,0,0,8" VerticalAlignment="Top">
            <Grid.ColumnDefinitions><ColumnDefinition Width="150" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <TextBlock Margin="0,5,8,0" FontWeight="SemiBold" Text="Nom du fichier" />
            <TextBox x:Name="FileNameTextBox" Grid.Column="1" Padding="5,3" />
        </Grid>

        <Grid Grid.Row="7" Margin="0,10,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <TextBlock x:Name="ValidationText" Margin="0,6,8,0" Foreground="#FFB3B3" TextWrapping="Wrap" />
            <WrapPanel Grid.Column="1">
                <Button Click="OnConfirmClick" Content="Valider" IsDefault="True" />
                <Button Click="OnCancelClick" Content="Annuler" IsCancel="True" />
            </WrapPanel>
        </Grid>
    </Grid>
</Window>
