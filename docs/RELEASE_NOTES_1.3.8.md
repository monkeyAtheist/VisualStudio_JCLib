# JC Lib Visual Studio 1.3.8 — environment and OpenSSH alias compatibility

- Synchronize bundled `system_scripting_pack.json` to `1.9.0` and `default_pack.json` metadata to `2.18.0`.
- Import environment-path helpers, local hosts-file aliases and OpenSSH Host aliases.
- Extend console-execution confirmation guards for persistent environment changes, hosts-file edits and public-key enrollment.
- Keep integrated-pack generation exclusive to VS Code.
