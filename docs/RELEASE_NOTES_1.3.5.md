# JC Lib Visual Studio 1.3.5

- Synchronisation du pack Build `1.2.2`.
- Prévisualisation des options GCC de recherche avec espacement explicite : `-I <directory>` et `-L <directory>`.
- Confirmation que `StructuredChoiceDialog` ferme déjà la popup via `DialogResult = true` au clic sur `Appliquer`.
