# JC Lib Visual Studio 1.3.4

- Reclassifies Build tooling cards as `command`.
- Renders GCC, G++, Clang, MinGW, CMake, Make and Ninja previews as commands.
- Removes irrelevant return-target fields for Build command cards.
- Prevents semicolon suffixes from being appended to shell and CMake commands.
