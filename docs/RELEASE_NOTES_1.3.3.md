# JC Lib Visual Studio 1.3.3

- Synchronisation du fallback `default_pack.json` 2.07.0.
- Ajout de `Assets/Packs/build_pack.json` 1.2.0.
- Prise en charge de `enabledWhen` pour désactiver conditionnellement les éditeurs de paramètres.
- Prise en charge de `pickerConfig.defaultTargetIndex` afin qu'un choix documenté puisse initialiser un champ associé.
- Séparation des paramètres GCC/G++/MinGW : `-I`, `-L`, `-lfoo` et options du linker.
