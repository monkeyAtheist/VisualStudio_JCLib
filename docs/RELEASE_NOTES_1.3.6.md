# JC Lib Visual Studio 1.3.6

- Import des packs library-first avec tableau racine `libraries`.
- Normalisation visuelle des anciens environnements vers `Library → Category → Group → Symbol`.
- Création de packs library-first sans couche d'environnement visible.
- Synchronisation du pack Build `1.2.3` et du fallback `2.10.0`.
- Validation des choix multiples obligatoires avec restauration de la valeur par défaut.
- Désactivation visuelle des choix incompatibles dans la boîte WPF structurée.
